import { redirect } from "next/navigation";

export default function AdminLoginPage() {
  redirect("/en/login");
  redirect("/hi/login");
  redirect("/km/login");
  redirect("/mr/login");
  redirect("/te/login");

}
